<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
    <ul>
        <% users.forEach(function(name) { %>
            <li><%= name %></li>
        <% }); %>
    </ul>
</body>
</html>